prototype(Carbon.IncludeAssets:Internal.Tag.CSS) {
    @context {
        path = ${this.path}
    }

    @cache {
        mode = 'cached'
        entryIdentifier {
            hash = ${path}
        }
        entryTags {
            1 = 'Carbon_Include_Assets'
        }
    }
}